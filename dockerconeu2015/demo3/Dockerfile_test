# This is a test block
@AFTER RUN_USERADD
ASSERT_TRUE USER_EXISTS 'tomcat'
ASSERT_TRUE FILE_EXISTS '/home/tomcat/.profile'

# Another test block
@BEFORE COPY_HELLO
ASSERT_FALSE FILE_EXISTS '/usr/local/tomcat/webapps/hellorouen.war'

# Last test block
@AFTER COPY_HELLO
ASSERT_TRUE FILE_EXISTS '/usr/local/tomcat/webapps/hellorouen.war'
